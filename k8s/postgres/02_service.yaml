apiVersion: v1
kind: Service
metadata:
  name: postgres-service
spec:
  type: ClusterIP  # Делаем сервис доступным только внутри кластера
  ports:
    - port: 5432  # Внутренний порт базы данных
  selector:
    app: postgres